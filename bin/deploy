#!/bin/sh

# Run this script to deploy the app to Heroku.

set -e

branch="$(git symbolic-ref HEAD --short)"
echo $branch
target="${1:-staging}"
echo $target

echo $"$target" "$branch:develop"

# git push "$target" "$branch:develop"
